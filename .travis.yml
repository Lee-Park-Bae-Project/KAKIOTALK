language: node_js

node_js:
  - "lts/*"
cache:
  directories:
    - node_modules
env:
  - CI=false
before_script:
  - yarn install
  - yarn common:build
  - yarn client:build
  - cd packages
  - cd client
script:
  - echo "skipping tests"
  # - yarn deploy
deploy:
  provider: pages
  github_token: $__GITHUB_TOKEN__
  skip_cleanup: true
  local_dir: packages/client/build
  on:
    branch: feature/split-main
